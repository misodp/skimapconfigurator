/**
 * Unified canvas input adapter.
 * Uses the pointer-based interaction pipeline so touch + mouse can coexist.
 */
export { attachMobileCanvasInput as attachUnifiedCanvasInput } from '../mobile/canvas-input-mobile.js';
