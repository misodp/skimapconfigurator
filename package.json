{
  "name": "ski-map-configurator",
  "private": true,
  "version": "0.9.6 Alpha",
  "type": "module",
  "scripts": {
    "encrypt-assets": "node scripts/encrypt-s67.mjs",
    "dev": "npm run encrypt-assets && vite",
    "build": "npm run encrypt-assets && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.3"
  }
}
